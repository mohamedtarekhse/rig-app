{
  "name": "rigways-acm",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev":    "wrangler pages dev . --proxy 8787",
    "deploy": "wrangler pages deploy .",
    "build":  "echo 'No build step required'",
    "test":   "node tests/ux-plan-smoke.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}
